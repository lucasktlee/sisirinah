{{!< default}}
<article class="{{post_class}}">
	{{#post}}
		<header class="post-header">
			<h1>{{{title}}}</h1>
		</header>
		<section class="post-content">
			{{content}}
		</section>
	{{/post}}
</article>